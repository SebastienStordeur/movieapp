import React from "react";
import Navbar from "../../Navbar/Navbar";

const Header: React.FC = () => {
  return (
    <header id="header">
      <Navbar />
    </header>
  );
};

export default Header;
